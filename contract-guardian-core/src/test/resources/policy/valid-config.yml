version: "1"

sources:
  kafka:
    paths:
      - "schemas/kafka/**/*.avsc"
    baseline: branch:main

rules:
  kafka:
    compatibility: BACKWARD
    overrides:
      - topic: "internal.*"
        compatibility: NONE

gate:
  block-on: breaking
