version: "1"

sources:
  kafka:
    paths:
      - "schemas/kafka/**/*.avsc"
    baseline: branch:main

rules:
  kafka:
    compatibility: BACKWARD
    # n-version-compatibility: 1  # check against last N committed versions (default: 1)

gate:
  block-on: breaking
